{
  "name": "agent-sdk-hsm",
  "version": "1.0.0",
  "description": "Claude Agent SDK with HSM state machine tool orchestration",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "manifest:generate": "node scripts/source-manifest.mjs generate",
    "manifest:verify": "node scripts/source-manifest.mjs verify",
    "manifest": "npm run manifest:generate && npm run manifest:verify"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  }
}
